{
  "id": "card-source-def-kinds",
  "meta": {
    "title": "Available Source-Def Kinds",
    "ingest": true,
    "tags": [
      "reference",
      "source-defs"
    ],
    "desc": "Reference card listing all source-def kinds supported by the task executor, with required fields and usage notes."
  },
  "view": {
    "elements": [
      {
        "kind": "narrative",
        "data": {
          "bind": "card_data.summary"
        }
      },
      {
        "kind": "table",
        "data": {
          "bind": "card_data.kinds_table"
        }
      }
    ],
    "layout": {
      "board": {
        "col": 4,
        "order": 99
      },
      "canvas": {
        "x": 50,
        "y": 50,
        "w": 600,
        "h": 800
      }
    }
  },
  "card_data": {
    "summary": "Each source_def entry requires `bindTo` (string) and `outputFile` (string). Use `mcp` for hosted integrations such as Teams Graph tools.",
    "kinds_table": [
      {
        "kind": "urls",
        "description": "URL fetch with {{key}} interpolation. Supports caching and projected forEach fan-out.",
        "required_fields": "urls.url (string)",
        "notes": "Optional: urls.method, urls.headers, urls.args, urls.cacheTimeout, urls.projectionList, tickersFrom."
      },
      {
        "kind": "copilot",
        "description": "Invoke GitHub Copilot CLI with an interpolated prompt template.",
        "required_fields": "copilot.prompt_template (string)",
        "notes": "Optional: copilot.args, copilot.result_shape. Auto-sandboxed."
      },
      {
        "kind": "mcp",
        "description": "Invoke an MCP tool via a manifest-selected or explicitly configured MCP server.",
        "required_fields": "mcp.tool (string)",
        "notes": "Optional: mcp.manifest, mcp.server, mcp.input, mcp.args. Use this for hosted integrations such as Teams Graph and WorkIQ."
      }
    ],
    "common_source_def_fields": {
      "bindTo": "Namespace key — result lands in fetched_sources.<bindTo>",
      "outputFile": "Board-relative path the executor writes result to",
      "timeout": "Fetch timeout in ms (default: 120000)",
      "projections": "Object of JSONata expressions pre-computing _projections from requires data",
      "optionalForCompletionGating": "If true, card completes even if this source fails (default: false)"
    }
  }
}